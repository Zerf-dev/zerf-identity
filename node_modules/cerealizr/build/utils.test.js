"use strict";

var _utils = require("./utils");

var _serializer = _interopRequireDefault(require("./serializer"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('setValue', function () {
  var someObject = {
    id: 1
  };
  var descriptor = {
    id: (0, _utils.setValue)({
      someKey: 100,
      someOtherKey: 'aString'
    })
  };
  var serializer = new _serializer.default({
    descriptor: descriptor
  });
  it('sets a hardcoded value as the id', function () {
    expect(serializer.serialize(someObject).id).toEqual({
      someKey: 100,
      someOtherKey: 'aString'
    });
  });
});
describe('setCamelcaseKey', function () {
  var someObject = {
    snake_case_key: 1,
    other_key: 'other value'
  };
  var descriptor = {
    snake_case_key: (0, _utils.setCamelcaseKey)(function (value) {
      return value + 100;
    })
  };
  var serializer = new _serializer.default({
    descriptor: descriptor
  });
  it('transform key to camelcase and transforms the value with the given function', function () {
    expect(serializer.serialize(someObject).snakeCaseKey).toBe(101);
  });
});